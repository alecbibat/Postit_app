<%= render "shared/header", title: "New Category" %>

<%= render "shared/error", obj: @category %>

<%= form_for @category do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.submit 'Create Category', :class => 'btn btn-primary' %>
<% end %>